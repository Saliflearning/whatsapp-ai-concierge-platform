# ADR 0002: Deterministic grounding and handoff

**Status:** Accepted

The reference uses an inspectable approved-source matcher instead of a hosted language model. A matching source produces a cited answer; no match or a sensitive claim produces a reason-coded handoff. This demonstrates the control plane without implying model quality or requiring paid services.
