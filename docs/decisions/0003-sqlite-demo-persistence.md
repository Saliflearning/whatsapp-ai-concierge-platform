# ADR 0003: SQLite for reproducible persistence

**Status:** Accepted

SQLite provides transactions, foreign keys, unique provider-event constraints, and a zero-service quickstart. It is a deliberate demo boundary, not a recommendation for multi-region messaging workloads. The repository interface preserves a migration seam.
